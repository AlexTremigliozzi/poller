<p id="notice"><%= notice %></p>

<h1>Listing My Orders</h1>

<table>
  <thead>
    <tr>
      <th>Data1</th>
      <th>Data2</th>
      <th>Data3</th>
      <th>Name</th>
      <th>Desc</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @my_orders.each do |my_order| %>
      <tr>
        <td><%= my_order.data1 %></td>
        <td><%= my_order.data2 %></td>
        <td><%= my_order.data3 %></td>
        <td><%= my_order.name %></td>
        <td><%= my_order.desc %></td>
        <td><%= link_to 'Show', my_order %></td>
        <td><%= link_to 'Edit', edit_my_order_path(my_order) %></td>
        <td><%= link_to 'Destroy', my_order, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New My order', new_my_order_path %>

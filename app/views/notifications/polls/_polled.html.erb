<li>
  <%= link_to poll_path(notification.notifiable, anchor: dom_id(notification.notifiable)), class: "dropdown-item #{"unread" if !notification.read_at?}" do %>
    <%= notification.actor.name %> ha inserito un sondaggio
    <!-- %= notification.notifiable_type.underscore.humanize.downcase %-->
    <br/>
    <i class="pull-right"><%= time_ago_in_words notification.created_at  %> fa</i><br/>
  <% end %>
</li>
